<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:util="http://www.springframework.org/schema/util"
	xsi:schemaLocation="http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util-4.3.xsd">
	
	<!-- Helper 구동에 필요한 환경설정 값 -->
	<util:properties id="config">
		<!-- 기본 인코딩 타입 -->
		<prop key="encType">UTF-8</prop>
		<!-- 업로드 디렉토리 -->
		<prop key="uploadDir">D:\project\FDP\upload</prop>
		<!-- 최대 업로드 가능 용량 -->
		<prop key="uploadMaxFileSize">1000000</prop>
		<!-- 기본 도메인 (쿠키설정용) -->
		<prop key="domain">localhost</prop>
		<!-- 메일발송 서버 주소 -->
		<prop key="smtpHost">smtp.gmail.com</prop>
		<!-- 메일발송 서버 포트 -->
		<prop key="smtpPort">465</prop>
		<!-- 메일발송 서버 계정 -->
		<prop key="smtpUsername">mkultra0115@Gmail.com</prop>
		<!-- 메일발송 서버 비밀번호 -->
		<prop key="smtpPassword">rkxbedhrmvjdjawd</prop>
	</util:properties>
	
	
	<!-- WebHelper 주입 설정 -->
	<bean id="webHelper" class="fdp.project.spring.helper.WebHelper">
		<property name="encType" value="#{config['encType']}" />
		<property name="uploadDir" value="#{config['uploadDir']}" />
		<property name="uploadMaxFileSize">
			<value type="java.lang.Long">
				#{config['uploadMaxFileSize']}
			</value>
		</property>
		<property name="domain" value="#{config['domain']}" />
	</bean>
	
	<!-- DownloadHelper 주입 설정 -->
	<bean id="downloadHelper" class="fdp.project.spring.helper.DownloadHelper">
		<property name="uploadDir" value="#{config['uploadDir']}"></property>
	</bean>
	
	<!-- 메일 발송에 필요한 정보를 저장하는 객체 정의 -->
	<bean id="mailSender" class="org.springframework.mail.javamail.JavaMailSenderImpl">
		<property name="host" value="#{config['smtpHost']}" />
		<property name="port" value="#{config['smtpPort']}" />
		<property name="username" value="#{config['smtpUsername']}" />
		<property name="password" value="#{config['smtpPassword']}" />
		<property name="defaultEncoding" value="#{config['encType']}" />
		<property name="javaMailProperties">
			<props>
				<prop key="mail.smtp.starttls.enable">true</prop>
				<prop key="mail.smtp.auth">true</prop>
				<prop key="mail.smtp.debug">true</prop>
				<prop key="mail.smtp.socketFactory.port">#{config['smtpPort']}</prop>
				<prop key="mail.smtp.socketFactory.class">javax.net.ssl.SSLSocketFactory</prop>
				<prop key="mail.smptp.socketFactory.fallback">false</prop>
			</props>
		</property>
	</bean>
	
	 <!-- Mail Helper 주입 설정 -->
    <bean id="mailHelper" class="fdp.project.spring.helper.MailHelper">
        <constructor-arg ref="mailSender" />
    </bean>
	
	<!-- RegexHelper 주입 설정 -->
	<bean id="regexHelper" class="fdp.project.spring.helper.RegexHelper" />
	<!-- RetrofitHelper 주입 설정 -->
	<bean id="retrofitHelper" class="fdp.project.spring.helper.RetrofitHelper" />
	
	
	<!-- Root Context: defines shared resources visible to all other web components -->
		
</beans>
